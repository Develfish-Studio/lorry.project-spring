spring:
  application:
    name: {{module.name}}
  config:
    import:
      - optional:classpath:service-{{module.name}}.yaml

springdoc:
  api:
    title: {{api.title}}
    description: {{api.description}}
    version: {{api.version}}
